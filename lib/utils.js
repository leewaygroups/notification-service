var utils = {
    isValidEvent: function (event) {
        //TODO: return true if valid otherwise return false
        return true;
    },

    isValidSubscription: function (subcr) {
        //TODO: return true if valid otherwise return false
        return true;
    },

    isValidEmail: function (email) {
        //TODO: return true if valid otherwise return false
        return true;
    },
    isValidEventUpdate: function (event, newEvent) {
        //TODO: return true if valid otherwise return false
        return true;
    }
};

module.exports = utils;